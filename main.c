#include "Transportadora.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
   
    int main() {
    ListaClientes lista;
    inicializaListaClientes(&lista);

    menu(&lista);

    return 0;
}

    return 0;
}